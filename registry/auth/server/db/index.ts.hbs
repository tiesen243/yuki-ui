import type { Database } from '@/server/db/schema'

const globalForMemory = globalThis as unknown as {
  db: Database | undefined
}
export const db = globalForMemory.db ?? {
  users: [],
  accounts: [],
  sessions: [],
}
if (process.env.NODE_ENV !== 'production') globalForMemory.db = db
